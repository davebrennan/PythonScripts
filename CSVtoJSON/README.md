This script allows to to convert a csv file into a JSON file. While also letting you choose to change the delimiter

To run the script execute in terminal
python3 csvtojson.py input.csv

will create output.json
